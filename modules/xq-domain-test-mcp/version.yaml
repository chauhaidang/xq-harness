schema_version: 1
version: 1.0.3
mirrors:
  - path: pyproject.toml
    format: pyproject-project-version
changelog:
  - version: 1.0.3
    changes:
      - Baseline imported into the module release manifest.
