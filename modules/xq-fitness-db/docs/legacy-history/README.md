# Legacy standalone SQL history

These SQL files are preserved source evidence from the standalone fitness
database. They are documentation inputs for issue #47 and must not be replayed,
mounted into PostgreSQL initialization, or used as a production migration
ledger. The verified Prisma baseline will become migration authority.
