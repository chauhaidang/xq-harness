/**
 * Reads and parses a YAML file
 * @param filePath - Path to the YAML file to read
 * @returns The parsed YAML content, or null if an error occurs
 */
export declare function readYAML(filePath: string): any | null;
