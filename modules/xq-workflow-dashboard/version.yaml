schema_version: 1
version: 0.1.0
mirrors:
  - path: package.json
    format: package-json
  - path: package-lock.json
    format: package-lock-json
changelog:
  - version: 0.1.0
    changes:
      - Baseline imported into the module release manifest.
