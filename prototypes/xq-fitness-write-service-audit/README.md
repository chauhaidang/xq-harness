# XQ Fitness write-service audit candidate

This non-publishable prototype is the corrected dependency-only candidate for
GitHub issue #44. It preserves the archived runtime and development dependency
surface while applying the approved deterministic changes:

- replace `@chauhaidang/xq-test-utils@1.0.2` with exact published
  `@chauhaidang/xq-harness-test-utils@0.1.1`;
- keep exact `@chauhaidang/xq-harness-common-kit@0.1.0`;
- pin `ts-node@10.9.2` instead of `latest`; and
- upgrade `jest-junit` to exact `17.0.0`, which removes the vulnerable
  `uuid@8.3.2` path reported by the first authorized audit run; and
- retain the generated OpenAPI client as a local, private package stub with the
  dependency contract generated by OpenAPI Generator `7.17.0`.

It is audit evidence, not the source import from issue #46. The CI workflow
generates a candidate lock when none is committed, runs separate production and
full-development npm advisory queries, checks clean installation and registry
deprecations, and retains sanitized evidence plus the candidate lockfile. No
archived `.npmrc`, source code, credentials, or production data is included.

The committed candidate lockfile was generated by the authorized CI runner
after the `jest-junit@17.0.0` remediation. The workflow consumes that exact lock
and records its SHA-256, binding final audit evidence to the reviewed dependency
graph.
